{% extends "@Admin/base_list.html.twig" %}

{% block title %}
    <div class="page-header">
        <h1>Module utilisteurs <small>- Liste des utilisateurs</small></h1>
    </div>
{% endblock %}

{% block list_thead %}
    <tr>
        <th>#</th>
        <th>Nom</th>
        <th>Prénom</th>
        <th></th>
    </tr>
{% endblock %}

{% block list_tbody %}
    {% if users is defined %}
        {% for user in users %}
            <tr>
                <th scope="row">{{ user.id }}</th>
                <th class="title">{{ user.name }}</th>
                <th class="title">{{ user.lastName }}</th>
                <th class="action">
                    <div class="btn-toolbar" role="toolbar">
                        {#<a href="{{ path('DisplayBundle_Place_edit', {'pk': place.id}) }}" class="btn btn-xs btn-default" title="Editer le lieu">#}
                            {#<span class="glyphicon glyphicon-pencil"></span>#}
                        {#</a>#}
                        {#<a href="{{ path('DisplayBundle_Place_delete', {'pk': place.id}) }}"class="btn btn-xs btn-danger" data-function="confirmDel" data-msg="un lieu" title="Supprimer le lieu">#}
                            {#<span class="glyphicon glyphicon-trash"></span>#}
                        {#</a>#}
                    </div>
                </th>
            </tr>
        {% endfor %}
    {% endif %}
{% endblock %}

{% block aside %}
    <a href="{{ path('fos_user_registration_register') }}" class="btn btn-block btn-primary btn-lg">
        <span class="glyphicon glyphicon-plus"></span> Ajouter un utilisateur
    </a>
{% endblock aside %}
