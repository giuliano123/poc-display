# Learn more about services, parameters and containers at
# https://symfony.com/doc/current/service_container.html
parameters:
    #parameter_name: value

services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: false

    # add more services, or override services that need manual wiring
    # AppBundle\Service\ExampleService:
    #     arguments:
    #         $someArgument: 'some_value'

    display.file.extension:
        class: DisplayBundle\Extension\VichyTypeExtension
        tags:
            - { name: form.type_extension, extended_type: Vich\UploaderBundle\Form\Type\VichFileType }
        arguments: [ "@vich_uploader.templating.helper.uploader_helper" ]

#    display.file.uploader:
#        class: DisplayBundle\Uploader\FileUploader
#        arguments:
#            $targetDir: '%kernel.project_dir%/web/images/place'
#
#    display.place.upload.listener:
#        class: DisplayBundle\EventListener\PlaceUploadListener
#        arguments: [ "@display.file.uploader" ]
#        tags:
#            - { name: doctrine.orm.entity_listener }

#    display.event.upload.listener:
#        class: DisplayBundle\EventListener\EventUploadListener
#        arguments: [ "@display.file.uploader" ]
#        tags:
#            - { name: doctrine.orm.entity_listener }
